if status --is-interactive
	abbr -ag l "ls"
	abbr -ag ll "ls -l"
	abbr -ag la "ls -a"
	abbr -ag lla "ls -la"
	abbr -ag lll "ls -lah"
	abbr -ag c clear
	abbr -ag § pwd

	abbr -ag i "arch -x86_64"
end
